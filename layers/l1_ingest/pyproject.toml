[project]
name = "l1_ingest"
version = "0.1.0"
description = "Capa L1 de intrinsica: ingesta de aggTrades de Binance a Parquet."
requires-python = ">=3.14"
dependencies = [
    "dq",
    "duckdb>=1.5.5",
    "fsspec>=2026.9.0",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"

[tool.uv.sources]
dq = { workspace = true }
