[project]
name = "dq"
version = "0.0.0"
description = "Contrato del hallazgo de calidad de datos (DQ) de intrinsica."
requires-python = ">=3.14"
dependencies = ["pyarrow"]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
